version: '3.9'

services:
  hethongdatban:
    image: hethongdatban
    container_name: hethongdatban-container
    restart: always
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
